# Invision backend 

Fastify + Mongodb + NextAuth(Frontend)

## Quick start

1) clone repo
2) make '.env'
3) DONT FORGET INCLUDE DB NAME IN MONGODB_URI ("invision_university")
4) run:

```bash
npm install 
npm run dev
```